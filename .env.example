# =============================================================================
# Tramando - Configurazione Docker
# =============================================================================
# Copia questo file in .env e modifica i valori secondo necessità
# cp .env.example .env
# =============================================================================

# Porta su cui gira il server (esposta dal container)
PORT=3000

# Base path se servito sotto subpath (es. /tramando)
# Lasciare vuoto se servito dalla root
# Esempio: BASE_PATH=/tramando
BASE_PATH=

# Secret per la firma dei token JWT (CAMBIA IN PRODUZIONE!)
# Genera un valore sicuro con: openssl rand -hex 32
TRAMANDO_JWT_SECRET=change-me-in-production

# Abilita/disabilita la registrazione di nuovi utenti
# true = chiunque può registrarsi
# false = solo admin può creare utenti
TRAMANDO_ALLOW_REGISTRATION=true
